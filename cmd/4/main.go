package main

// ユーザー作成する処理フロー
func main() {
	// name, err  := user.NewFullName("yamanaka", "junichi")
	// if err != nil {
	// 	log.Fatal(err)
	// }

	// u := user.New(name)

	// if !user.Exists(u) {
	// 	log.Fatal("duplicate user name " + name.FullName())
	// }

	// ユーザー情報をDBに登録する処理が続く
}
